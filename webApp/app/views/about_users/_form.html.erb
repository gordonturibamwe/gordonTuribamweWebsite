<%= form_for(@about_user) do |f| %>
  <% if @about_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about_user.errors.count, "error") %> prohibited this about_user from being saved:</h2>

      <ul>
      <% @about_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-12">
    <%= f.text_field :name, class: "form-control", placeholder: "About Name - Resume, About me", autofocus: true %>
  </div>
  <div class="form-group col-md-12">
    <hr>
  </div>
  <div class="form-group col-md-6">
    <%= f.submit "Save", class: "btn btn-primary btn-md" %>
  </div>
<% end %>
